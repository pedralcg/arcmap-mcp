# Secretos / entorno
.env

# Diseño viejo (batch por subproceso) — referencia local, no se publica.
legacy/

# Backlog interno de pendientes — local, no se publica.
PENDIENTE.local.md

# Automatización local de desarrollo (defaults de la máquina del autor).
addin/tools/

# Artefactos de build .NET (el .esriaddin empaquetado sí se versiona en dist/).
addin/ArcmapMcp.AddIn/bin/
addin/ArcmapMcp.AddIn/obj/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Sistema operativo
.DS_Store
Thumbs.db
desktop.ini

# Editores / IDE
.vscode/
.idea/
*.swp

# Los logs de auditoría viven fuera del repo (C:\MCP_Logs por defecto).
